<!DOCTYPE html>
<html>
  <head>
    <title></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel='stylesheet' media='only screen and (min-width: 530px) and (min-device-width: 480px)' href='/stylesheets/wide.css' />
    <link rel='stylesheet' media='only screen and (max-width: 480px)' href='/stylesheets/smartphone.css' />
  </head>
  <body>
    <div id="page-wrap">
      <% if(!session.user){ %>
        <span><a href="/login">Login</a></span>
      <% }else{ %>
        <span class="username"><%= session.user.username %> <a href="/logout">Logout</a></span>
      <% } %>
      <header class="main" id="h1">
        <h2>Quiz: el juego de las preguntas</h2>
      </header>
      <nav class="main" id="h1" role="navigation">
        <span><a href="/">Inicio</a></span>
        <span><a href="/quizes">Preguntas</a></span>
      </nav>
      <section class="main" id="s1">
        <% if(errors.length){ %>
          <span id="errorMsg">Corregir errores:</span><br>
          <% for(var i in errors){ %>
            <span><%= errors[i].message %></span>
          <% } %>
        <% } %>
        <div class="">
          <%- body %>
        </div>
      </section>
      <footer class="main" id="f1">
        <a href="http://es.wikipedia.org/wiki/Quiz">¿Que es un Quiz?</a>
      </footer>
    </div>
  </body>
</html>
